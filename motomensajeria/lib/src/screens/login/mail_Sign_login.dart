import 'package:flutter/material.dart';

class MailSignin extends StatelessWidget {
  const MailSignin({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Hola Mundo')),
    );
  }
}